// bot token to access the API (get from @BotFather)
export const BOT_TOKEN = 'your-bot-token';

// your ID (you can check with @raw_data_bot)
export const BOT_CREATOR_ID = 0;

export default {
  BOT_TOKEN,
  BOT_CREATOR_ID,
};
